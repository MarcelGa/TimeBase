name: Build Core

on:
  push:
    branches: [main]
    paths:
      - 'src/TimeBase.Core/**'
      - 'src/TimeBase.Contracts/**'
      - 'src/TimeBase.Core.Infrastructure/**'
      - 'TimeBase.slnx'
      - '.github/workflows/build-core.yml'
  pull_request:
    branches: [main]
    paths:
      - 'src/TimeBase.Core/**'
      - 'src/TimeBase.Contracts/**'
      - 'src/TimeBase.Core.Infrastructure/**'
      - 'TimeBase.slnx'
      - '.github/workflows/build-core.yml'

jobs:
  build:
    name: Build .NET Core
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
        dotnet-version: ['10.0.x']
        include:
          - os: ubuntu-latest
            dotnet-version: '10.0.x'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ matrix.dotnet-version }}

      - name: Restore dependencies
        run: dotnet restore TimeBase.slnx

      - name: Build solution
        run: dotnet build TimeBase.slnx --configuration Release --no-restore

      - name: Run tests
        run: dotnet test TimeBase.slnx --configuration Release --no-build --verbosity normal --logger "trx;LogFileName=test-results.trx"

      - name: Upload test results
        uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: test-results-${{ matrix.os }}
          path: '**/*.trx'

      - name: Check code formatting
        run: dotnet format --verify-no-changes TimeBase.slnx
        continue-on-error: true

      - name: Build Docker image (ubuntu only)
        if: matrix.os == 'ubuntu-latest'
        run: |
          docker build -f docker/core/Dockerfile -t timebase/core:test .

      - name: Test Docker image (ubuntu only)
        if: matrix.os == 'ubuntu-latest'
        run: |
          docker run --rm timebase/core:test --version || echo "Version check completed"